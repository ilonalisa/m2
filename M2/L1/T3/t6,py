# Запитуємо, чи зроблені уроки
homework_done = input("Чи зроблені уроки? (так/ні): ")

# Якщо уроки зроблені, запитуємо про погоду
if homework_done.lower() == "так":
    weather_good = input("Чи гарна погода? (так/ні): ")
    
    # Виводимо відповідь в залежності від погоди
    if weather_good.lower() == "так":
        print("Гуляти!")
    else:
        print("Грати!")
else:
    # Якщо уроки не зроблені, рекомендація вчитися
    print("Вчитися!")
